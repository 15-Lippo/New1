[build]
  base = "."
  command = "pnpm install && pnpm turbo run build --filter=website^..."
  publish = "apps/website/.next"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--version"

[[plugins]]
  package = "@netlify/plugin-nextjs"
